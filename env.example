import { motion } from 'motion/react';
import { 
  Heart, 
  Coffee, 
  ExternalLink, 
  Zap, 
  ShieldCheck, 
  TrendingUp, 
  CheckCircle,
  CreditCard,
  Mail
} from 'lucide-react';
import { cn } from '../lib/utils';

export default function Support() {
  return (
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <div className="text-center max-w-3xl mx-auto mb-16">
        <h1 className="text-4xl md:text-6xl font-display font-bold mb-6">Support Helios</h1>
        <p className="text-xl text-brand-grey">
          Helios is free, unbiased, and dedicated to UK homeowners. 
          Help us keep the lights on (with solar, obviously).
        </p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div className="glass p-10 rounded-[40px] flex flex-col items-center text-center space-y-6 group hover:border-brand-yellow/30 transition-all">
          <div className="p-5 bg-brand-yellow/10 rounded-full text-brand-yellow mb-4 group-hover:scale-110 transition-transform">
            <Coffee size={32} />
          </div>
          <h3 className="text-2xl font-bold">Micro-Donation</h3>
          <p className="text-sm text-brand-grey leading-relaxed">
            Support our hosting costs for the Open-Meteo and Satellite APIs. 
            Every small contribution helps us stay independent.
          </p>
          <a 
            href="https://buy.stripe.com/8x23cvgl1cfm7xng4O8so00"
            target="_blank"
            rel="noopener noreferrer"
            className="w-full py-4 bg-brand-black text-white dark:bg-white dark:text-brand-black font-bold rounded-2xl flex items-center justify-center gap-2 hover:scale-105 transition-transform"
          >
            Buy Helios a Coffee <CreditCard size={18} />
          </a>
        </div>

        <div className="glass p-10 rounded-[40px] flex flex-col items-center text-center space-y-6 group border-brand-yellow/50 bg-brand-yellow/5 scale-105 shadow-2xl shadow-brand-yellow/10">
          <div className="p-5 bg-brand-yellow rounded-full text-brand-black mb-4 group-hover:scale-110 transition-transform">
            <Zap size={32} />
          </div>
          <h3 className="text-2xl font-bold">Switch to Octopus</h3>
          <p className="text-sm text-brand-grey leading-relaxed">
            Switch your energy provider using our referral link. 
            You get <span className="font-bold text-brand-black dark:text-white">£50 credit</span>, and Helios gets £50 to fund more tools.
          </p>
          <a 
            href="https://share.octopus.energy/new-grove-296" 
            target="_blank" 
            rel="noopener noreferrer"
            className="w-full py-4 bg-brand-yellow text-brand-black font-bold rounded-2xl flex items-center justify-center gap-2 hover:translate-y-[-2px] transition-all"
          >
            Claim £50 Credit <ExternalLink size={18} />
          </a>
        </div>

        <div className="glass p-10 rounded-[40px] flex flex-col items-center text-center space-y-6 group hover:border-brand-yellow/30 transition-all">
          <div className="p-5 bg-brand-yellow/10 rounded-full text-brand-yellow mb-4 group-hover:scale-110 transition-transform">
            <Heart size={32} />
          </div>
          <h3 className="text-2xl font-bold">Helios Support</h3>
          <p className="text-sm text-brand-grey leading-relaxed">
            Large-scale support helps us develop new features, maintain accuracy, 
            and expand our data intelligence hub.
          </p>
          <a 
            href="https://buy.stripe.com/eVq00jfgXbbiaJz05Q8so01"
            target="_blank"
            rel="noopener noreferrer"
            className="w-full py-4 bg-brand-black text-white dark:bg-white dark:text-brand-black font-bold rounded-2xl flex items-center justify-center gap-2 hover:scale-105 transition-transform"
          >
            Submit Donation <CreditCard size={18} />
          </a>
        </div>
      </div>

      <div className="mt-20 glass p-12 rounded-[40px] border-brand-grey-light/10 overflow-hidden relative">
        <div className="absolute -top-24 -right-24 w-72 h-72 bg-brand-yellow/10 rounded-full blur-[100px]" />
        
        <div className="max-w-xl mx-auto text-center space-y-8 relative z-10">
          <div className="p-4 bg-brand-yellow/10 w-fit mx-auto rounded-2xl text-brand-yellow">
            <Mail size={32} />
          </div>
          <h2 className="text-3xl md:text-5xl font-display font-bold">Join the Helios Intel</h2>
          <p className="text-brand-grey text-lg">
            Get early access to tech reports, tariff alerts, and UK energy policy updates. 
            No spam, just data.
          </p>
          <form 
            className="flex flex-col sm:row gap-4" 
            onSubmit={(e) => {
              e.preventDefault();
              const email = (e.currentTarget.elements.namedItem('email') as HTMLInputElement).value;
              window.location.href = `mailto:heliosheggie@gmail.com?subject=Join Helios Intel Request&body=I would like to join the Helios Intel data alerts mailing list.%0D%0A%0D%0ASubscriber Email: ${email}`;
            }}
          >
            <input 
              type="email" 
              name="email"
              placeholder="Enter your email" 
              className="flex-1 bg-white dark:bg-white/5 border border-brand-grey-light/20 rounded-2xl px-6 py-4 outline-none focus:border-brand-yellow transition-colors"
              required
            />
            <button 
              type="submit"
              className="px-10 py-4 bg-brand-yellow text-brand-black font-bold rounded-2xl hover:scale-105 active:scale-95 transition-all shadow-xl shadow-brand-yellow/10"
            >
              Sign Up
            </button>
          </form>
          <p className="text-xs text-brand-grey">
            For direct inquiries, contact <a href="mailto:heliosheggie@gmail.com" className="text-brand-yellow font-bold hover:underline">heliosheggie@gmail.com</a>
          </p>
        </div>
      </div>

      <div className="mt-20 glass p-12 rounded-[40px] border-brand-grey-light/10 overflow-hidden relative">
        <div className="absolute -top-24 -right-24 w-64 h-64 bg-brand-yellow/20 rounded-full blur-[80px]" />
        
        <div className="flex flex-col md:row items-center gap-12">
          <div className="flex-1 space-y-6">
            <h3 className="text-3xl font-display font-bold">Why support us?</h3>
            <div className="space-y-4">
              <div className="flex items-start space-x-3">
                <CheckCircle className="text-brand-yellow mt-1 shink-0" size={18} />
                <p className="text-brand-grey">No advertising from installers or manufacturers.</p>
              </div>
              <div className="flex items-start space-x-3">
                <CheckCircle className="text-brand-yellow mt-1 shink-0" size={18} />
                <p className="text-brand-grey">Open-source calculation engines for full transparency.</p>
              </div>
              <div className="flex items-start space-x-3">
                <CheckCircle className="text-brand-yellow mt-1 shink-0" size={18} />
                <p className="text-brand-grey">Built by owners, for owners. Truth over convenience.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
